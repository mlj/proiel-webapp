<% form_for(@user) do |form| %>
  <%= render :partial => 'form', :object => form %>
  <%= form.collection_select :role_id, Role.find(:all), :id, :description, :required => true, :label => 'Role' %>
  <%= form.submit "Update" %>
<% end %>
