<dl>
  <dt>Job ID:</dt><dd><%= @job.id %></dd>
  <dt>Run by:</dt><dd><%= link_to @job.user.login, @job.user %></dd>
  <dt>Tool name:</dt><dd><%= @job.name %></dd>
  <dt>Tool parameters:</dt><dd><%= @job.parameters %></dd>
  <dt>Affected source:</dt><dd><%= @job.source ? link_to(@job.source.code, @job.source) : '&nbsp;' %></dd>
  <dt>Audit trail:</dt><dd><%= @job.audited ? 'Yes' : 'No' %></dd>
  <dt>Started:</dt><dd><%=h @job.started_at ? @job.started_at.to_formatted_s(:db) : '?' %></dd>
  <dt>Finished:</dt><dd><%=h @job.finished_at ? @job.finished_at.to_formatted_s(:db) : '?' %></dd>
  <dt>Result:</dt><dd><%=h @job.result ? @job.result.to_s.capitalize : '?' %></dd>
</dl>

<b>Log output</b>

<pre>
<%=h @job.log %>
</pre>
