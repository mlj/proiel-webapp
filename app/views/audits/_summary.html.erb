<li>
  <%= link_to "Revision #{summary.version}", summary %> by <%= true || link_to_user(summary.user) %> at <%= summary.created_at %>
  <%= render(:partial => 'audits/diff', :object => summary)%>
</li>
