<% if diff.action == 'destroy' %>
  Removed
<% else %>
  <table class="diff">
    <% diff.changes.each do |attribute, (old, new)| %>
      <tr>
        <td><%= attribute %></td>
        <%= format_change(attribute, old, new) %>
      </tr>
    <% end %>
  </table>
<% end %>
