<% if diff.action == 'destroy' %>
  Removed
<% else %>
  <table class="diff">
    <% diff.changes.each do |attribute, values| %>
      <tr>
        <td><%= attribute %></td>
        <%= format_change(attribute, *values) %>
      </tr>
    <% end %>
  </table>
<% end %>
