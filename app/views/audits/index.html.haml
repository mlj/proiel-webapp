- if params[:sentence_id]
  Showing changes relating to sentence
  != params[:sentence_id]
- elsif params[:user_id]
  Showing changes by user
  != @user.login
- else
  Showing last week's changes

.tabular
  != paginate @audits

  %table
    %thead
      %tr
        %th Created by
        %th Created at
        %th Changed object

    %tbody
      != render @audits
