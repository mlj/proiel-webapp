<span class="warning">Experimental feature: Use with caution!</span>
<%= form.text_field :form %>
<%= form.text_field :composed_form %>
<%= form.text_field :sort %>
<%= form.text_field :morphtag %>
<%= form.collection_select :lemma_id, Lemma.find(:all, :conditions => [ "language = ? ", @token.language], :order => [ "lemma ASC" ]), :id, :presentation_form %>
