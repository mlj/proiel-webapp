<% roundedbox do -%>
  <%=h @source.citation %> <%= show_completion_rate(@source.completion, :checkmark => :only) %><br/>
  <dt>Title:</dt><dd class="title"><%=h @source.title %></dd>
  <dt>Abbreviation:</dt><dd class="title"><%=h @source.abbreviation %></dd>
  <dt>Tag:</dt><dd class="tag"><%=h @source.code %></dd>
  <dt>Language:</dt><dd><%= @source.language.name %></dd>
  <dt>Tracked references:</dt><dd class="tag"><%= @source.tracked_references.inspect %></dd>
  <%= link_to_resources @source, :index, :statistics, :edit %>
<% end %>

<% layer :metadata do -%>
  <%= @source.metadata.to_html %>
<% end %>
