%h2.title Texts

!= paginate @sources

%table.table
  %tbody
    - @sources.group_by(&:language_name).sort.each do |language_name, sources|
      - sources.each do |source|
        %tr
          %td!= link_to source.author_and_title, source
          %td= language_name
