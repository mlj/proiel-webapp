language: ruby
rvm:
  - 2.6.5
  - 2.5.7
  - 2.4.9
env:
  - DB=sqlite
  - DB=mysql
before_install:
  - gem update --system
  - gem install bundler:2.1.4
before_script:
  - cp config/database.yml.travis config/database.yml
script:
  - bundle exec bin/proiel-webapp
