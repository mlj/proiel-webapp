language: ruby
dist: xenial
os: linux
rvm:
  - 2.2.10
  - 2.4.10
  - 2.5.9
  - 2.6.8
services:
  - mysql
env:
  - DB=sqlite
  - DB=mysql
before_script:
  - cp config/database.yml.travis config/database.yml
  - mysql --version
  - bundle exec rake db:setup
script:
  - bundle exec rake
  - bundle exec rspec
